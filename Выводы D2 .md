Выводы 
### Часть 1. Исследование многомерного нормального распределения
### 1.1 Генерация данных с заданной ковариацией
В ходе выполнения задания были получены следующие результаты:

1. **Генерация независимых выборок**: Успешно сгенерированы два независимых вектора $x_1 \sim \mathcal{N}(0, 0.3^2)$ и $x_2 \sim \mathcal{N}(0, 0.8^2)$ по 500 точек каждый.

2. **Применение поворота**: Матрица поворота на угол $\alpha = 30°$ успешно применена к данным, что позволило создать коррелированную двумерную выборку.

3. **Ковариационная матрица**: Выборочная ковариационная матрица, вычисленная через `np.cov`, хорошо согласуется с теоретической матрицей $C_{теор} = R \cdot \text{diag}(\sigma_1^2, \sigma_2^2) \cdot R^T$.

4. **Визуализация**: На scatter plot чётко видна эллиптическая форма распределения данных, что соответствует теоретическим ожиданиям для многомерного нормального распределения с ненулевой ковариацией. Эллипсы равной плотности (на уровнях 0.5, 1, 2 стандартных отклонений) корректно описывают форму облака точек.

### 1.2 Сравнение с встроенной функцией

1. **Визуальное совпадение**: Облака точек, полученные ручным поворотом и встроенной функцией `np.random.multivariate_normal`, визуально совпадают. Оба метода генерируют данные с одинаковой ковариационной структурой.
Удобна тем, что не требует ручного построения матрицы поворота и диагональной матрицы дисперсий

3. **Проверки**: Все assert-проверки пройдены успешно:
   - Размерность данных корректна (500, 2)
   - Корреляция между признаками достаточна (> 0.1)
   - Среднее значение близко к нулю
